export enum ResponseCodes {
    SERVER_ERROR = 500,
    NOT_FOUND = 404,
    SUCESS = 200,
}


export enum ResponseNames {
    SUCCESS = "SUCCESS",
    SERVER_ERROR = "SERVER ERROR",
    NOT_FOUND = "NOT FOUND",
}